#!/bin/sh
# postinst script for abraflexi-reminder-papermail
set -e

. /usr/share/debconf/confmodule

    if [ "$1" = "configure" ]; then
        db_get composer/WEB_USER
        WEBUSER=$RET
        
        su - $WEBUSER -s /bin/bash -c 'COMPOSER_HOME="/var/lib/composer" composer  -o require listonoska/api -d /var/lib/composer/abraflexi-reminder/'
        su - $WEBUSER -s /bin/bash -c 'COMPOSER_HOME="/var/lib/composer" composer  -o require spipu/html2pdf -d /var/lib/composer/abraflexi-reminder/'

    fi


#DEBHELPER#

exit 0
